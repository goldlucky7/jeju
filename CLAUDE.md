# 제주 여행 스탬프북

부부 2인이 쓰는 여행 팸플릿 웹사이트. **`index.html` 파일 하나에 전부 들어 있다**
(빌드 도구·프레임워크 없음). `main`에 푸시하면 1~2분 뒤 GitHub Pages에 자동 반영된다.

배포 주소: https://goldlucky7.github.io/jeju/

## 사용자에 대해 (꼭 지킬 것)

- **비개발자, 주로 폰으로 확인한다.** 함수명·변수명이 아니라 "화면에서 뭐가 달라지는지"로 설명할 것
- **번거로운 걸 싫어한다.** 되묻지 말고 알아서 진행하고, 시작한 일은 `main`까지 올려서 끝낼 것
- 한국어로 소통
- **작업이 `main`에 올라가야 사이트에 보인다.** 기능 브랜치에만 올려두면 "업데이트가 안 됐다"는 말을 듣는다

## 파일

| 파일 | 내용 |
|---|---|
| `index.html` | 사이트 전체 (장소 데이터 + 화면 + 로직) |
| `updates.json` | 🆕 요즘 뜨는 곳 피드 |
| `lib/zxing.min.js` | 탑승권 인식기 (QR·PDF417·Aztec). 스캔 버튼을 눌렀을 때만 읽는다 |
| `lib/jsQR.js` | 탑승권 인식기 예비 (QR 전용) |
| `tools/` | 탑승권 인식 검증 도구 (사이트에서 쓰지 않음) |

`lib/`는 **지우면 안 된다.** 외부 CDN 대신 여기서 먼저 읽는데, 없으면 통신망이나
광고 차단기 뒤에서 탑승권 인식이 통째로 죽는다.

## 지금 상태 (2026-08-30)

장소 295곳 · 세부 칩 17개.

**첫 화면 구조 (2026-08-30k 전면 정리)** — 예전엔 큰 색 버튼과 떠다니는 버튼이 뒤엉켜 있었다:
`머리말(짧게)` → `장소 종류 탭(한 줄 가로 스크롤)` → `검색+칩` → `내 여행(타일 3)` →
`여행 도구(타일 6)` → `골라 보기(접이식 2)` → `사용법(접이식)` → `목록`

- 종류 탭은 **한 줄**이다. 여러 줄로 감싸지 말 것 — 12개가 구름처럼 흩어져 정신없었다
- 내 여행(`#myRow`)·여행 도구(`#toolGrid`)는 **아이콘+이름 타일**로 모양을 통일한다.
  큰 그라데이션 버튼을 다시 만들지 말 것 — 전부 똑같이 소리쳐서 뭐가 중요한지 안 보인다
- **떠다니는 버튼(`position:fixed`)을 다시 넣지 말 것.** 본문을 가려서 정신없던 주범이다
  (`#fabAdd`·`#fabHours`는 이제 도구 타일이다)
- 탭이 `#tabs`와 `#myRow` 두 곳에 나뉘어 있어서 **클릭은 `document`에서 받는다**
- `nearbyBtn`·`updBtn`은 JS가 글자를 바꾸므로 타일 모양(`<span class="t-ico">`)을 유지해야 한다

**🎟 할인 쿠폰(`COUPONS`)** — 2026-08-30에 롯데렌터카 쿠폰팩을 빼고 대신 넣었다.
전부 **출처를 확인한 것만** 담는다(지어내기 금지). 지금 4가지:
나우다(제주 공식 디지털 관광증) · 탐나는전(제주 지역화폐) · 문화가 있는 날 · 성산일출봉 무료 관람의 날.
혜택·기간·비율은 자주 바뀌므로 화면에 **"조사 시점 기준"을 반드시 함께** 적을 것.
- 롯데 데이터(`LOTTE_ALL`)와 저장 칸 `r`(`rates`)은 **지우지 않았다.** 병합에서 `r`을 빠뜨리면
  상대방 기기의 기록이 날아간다. 화면만 뺀 것이라 되살리기도 쉽다 최근에 ✈️ 항공편 기능이 들어왔다.

- `188adc2` 항공사 운영시간 · 탑승권 QR 인식 기능 추가
- `603523e` 카메라를 켜도 인식이 안 되던 문제 수정 + 인식기를 `lib/`에 포함
- `67f8ba1` 인쇄된 탑승권 인식률 개선 + 사진 경로 인식기 초기화 버그 수정
- **실제 t'way 종이 탑승권이 안 읽히던 문제 수정** (아래 함정 표 마지막 세 줄)
- 스캔 중 화면 아래에 **상태줄**이 뜬다 (`인식기 이름 · 확인 횟수 · 화면 크기 · 밝기`).
  (카메라 화면 **위에 겹쳐서** 나오므로 스캔 화면을 찍으면 무조건 같이 찍힌다)
  안 읽힌다는 제보가 오면 **이 줄을 찍어 보내달라고 할 것.** 한 줄로 원인이 갈린다:
  확인 횟수가 안 늘면 루프가 멎은 것 · 밝기 0이면 카메라 그림이 안 넘어오는 것 ·
  `기기내장(응답없음·끔)`이면 내장 인식기가 답을 안 준 것

화면 아래쪽에 `화면 버전 2026-08-30m`가 찍혀 있다. 사용자가 예전 화면(캐시)을 보고
있는지 확인할 때 쓴다. **화면을 고치면 이 숫자도 같이 올릴 것.**

## ⛔ 카메라로 계속 비추는 스캔은 화면에서 뺐다 (2026-08-30i)

**여섯 번을 고쳐도 사용자 폰(갤럭시 노트20)에서 끝내 안 읽혔다.** 마지막 원인은
**미리보기 화면 자체가 흐린 것**이었고(실측 또렷함 13.9 ≈ 2.7px 뭉개짐), 이건 웹페이지가
어떻게 할 수 있는 영역이 아니다. 같은 폰으로 **찍은 사진은 잘 읽힌다.**

지금 방식은 **[📸 탑승권 찍어서 읽기]** 하나다. 폰 사진 앱을 열어서(`capture="environment"`)
찍게 하고, 그 사진을 기존 사진 인식 경로(`decodeImageFile`)에 태운다. 사진 앱은 초점을
제대로 잡으므로 확실하다.

- 스캔 코드(`startScan` · `pickEngine` · `#flScan` · 또렷함 표시 등)는 **지우지 않고 남겨뒀다.**
  되살리려면 버튼 하나와 연결 한 줄만 넣으면 된다 (`flScanBtn` 자리를 찾을 것)
- **다시 넣자는 얘기가 나오면 먼저 재보게 할 것.** 다른 기기에서는 잘 됐을 수도 있다
- 아직 안 해본 마지막 수단: `ImageCapture.takePhoto()` — 카메라를 연 채로 "사진 품질"
  프레임을 받아오는 것. 되면 자동 인식이 살아나지만 기기마다 지원이 들쭉날쭉하고
  느리다. 사용자와 상의 없이 시도하지 말 것 (이미 여섯 번 헛수고했다)

## 사용자가 "안 읽힌다"고 하면 — 여기부터 볼 것

**다섯 번 연속으로 못 고쳤다.** 매번 다른 원인이었고, 화면 너머를 못 보니 추측이 계속 빗나갔다.
지금은 화면에 **초점 상태와 또렷함 점수**가 뜨므로 **그 화면을 찍어달라고 먼저 요청할 것.**

| 화면에 뜨는 것 | 뜻 | 할 일 |
|---|---|---|
| `또렷함 55 이상` + 안 읽힘 | 초점은 맞는데 못 읽음 | 진짜 인식 문제. 사진을 받아 `tools/` 로 재현할 것 |
| `또렷함 32 미만` | **초점이 안 맞음 — 가장 흔한 원인** | 화면 톡 누르기 · 조금 멀리 떼기 · `[📸 찍어서 읽기]` 안내 |
| `확인 횟수`가 안 늘어남 | 인식 루프가 멎음 | 어딘가 `await` 가 안 끝나는 것. `withTimeout` 확인 |
| `밝기 0` | 카메라 그림이 캔버스로 안 넘어옴 | `drawImage(video)` 문제. 완전히 다른 갈래 |
| `기기내장(응답없음·끔)` | 내장 인식기가 답을 안 줌 | 이미 처리됨(우리 인식기로 넘어감) |

**`[📸 찍어서 읽기]`가 가장 확실한 길이다.** 폰 사진 앱이 초점을 제대로 잡아주므로,
미리보기가 흐려서 안 되는 폰에서도 통한다. 안 읽힌다는 제보엔 이것부터 안내할 것.

## 남은 일 / 확인 필요

- [x] **사용자의 실제 t'way 종이 탑승권 사진으로 확인 완료.** 촬영 거리를 바꿔가며 5가지로
      돌려서 전부 읽힌다 (0.5~3초). 사용자 기기는 **갤럭시 노트20(안드로이드)**
      — 기기 내장 인식기를 쓰는 갈래라 그 갈래가 고장 났을 때가 문제였다
- [ ] **탑승권 사진은 저장소에 넣지 않았다.** 예약번호·편명이 들어 있어서다.
      또 검증할 일이 생기면 사용자에게 사진을 다시 받아 `/tmp`에서만 쓸 것
- [ ] **심하게 흐린 종이 PDF417은 못 읽는다** (검증에서 4/20 실패). ZXing 라이브러리 한계다.
      국소 적응 이진화(`adaptiveBin`)를 넣어도 이 4장은 그대로 실패했다.
      안드로이드 크롬은 기기 내장 인식기를 써서 이 경우도 대부분 읽는다.
      더 손보려면 내장 인식기가 없는 기기용 PDF417 처리를 따로 짜야 하는데, 비용 대비 효과를
      사용자와 먼저 상의할 것
- [ ] `goldlucky7/Happy`에 오래된 브랜치 5개가 남아 있다 (정리할지 사용자에게 물어본 상태)

## 손보기 전에 읽을 것 — 이미 밟은 함정

같은 걸 또 밟지 않도록 적어둔다. **아래는 전부 실제로 겪은 문제다.**

| 증상 | 원인 | 지금 어떻게 해결돼 있나 |
|---|---|---|
| 카메라는 켜지는데 아무리 비춰도 반응 없음 | ZXing 자체 영상 기능이 `playing` 이벤트를 기다리는데, 우리가 카메라를 먼저 켜서 그 이벤트가 이미 지나감 | ZXing 영상 기능을 안 쓰고 프레임을 직접 떠서 넘긴다 (`pickEngine` 단일 루프) |
| 인식기가 프레임을 못 읽음 | ZXing의 `decode(video)`가 화면을 제대로 못 떠옴 | 캔버스로 떠서 `HTMLCanvasElementLuminanceSource`로 넘긴다 (`zxDecodeCanvas`) |
| 아이폰에서 카메라 권한이 조용히 거부됨 | 인식기 파일(수백 KB)을 먼저 받느라 버튼 누른 시점이 만료 | **카메라부터 열고** 인식기를 나중에 고른다 |
| 통신망·차단기 뒤에서 인식 불가 | 인식기를 외부 CDN에서 받아옴 | `lib/`에 넣어 저장소에서 먼저 읽는다 |
| [🖼 사진에서 찾기]가 QR만 겨우 읽음 | `flReader`를 카메라 켤 때만 만들어서 사진 경로에선 늘 비어 있었음 | `decodeImageFile`에서도 없으면 만든다 |
| 인쇄된 탑승권이 안 읽힘 | 한 가지 방식으로만 봐서 놓침 | 프레임마다 5가지를 번갈아 시도 (`SCAN_PASSES`) |
| 입력하던 값이 사라짐 | 값이 바뀔 때마다 폼을 통째로 다시 그림 | 폼은 그대로 두고 **미리보기만** 다시 그린다 (`renderFlightPreview`) |
| **안드로이드에서 아무리 비춰도 안 읽힘** (실제로 겪은 증상) | 기기 내장 인식기를 한 번 고르면 **갈아탈 방법이 없었다.** Play 서비스의 바코드 부품이 없으면 내장 인식기가 오류도 없이 "못 찾았다"만 영원히 돌려준다 | 내장 인식기만 쓰지 않고 **매 프레임 내장 → ZXing 순으로 둘 다** 돌린다 (`pickEngine` 하나로 합침) |
| **위를 고쳤는데도 여전히 안 읽힘** (같은 폰에서 두 번째로 겪은 증상) | 내장 인식기가 빈손을 주는 게 아니라 **아예 답을 안 준다.** `await det.detect(v)`가 영영 안 끝나서 스캔 루프가 첫 프레임에서 멈췄다. `getSupportedFormats()`가 멎으면 `pickEngine`이 안 끝나 **스캔이 시작조차 못 한다** | 내장 인식기 호출을 전부 `withTimeout`으로 감쌌다. 제한 시간을 두 번 넘기면 내장 인식기를 **아예 끄고** 우리 인식기만 쓴다 |
| **그래도 여전히 안 읽힘** (같은 폰에서 세 번째) | **너무 느려서** 반응이 없는 것처럼 보였다. 탑승권에 쓰지도 않는 형식(DataMatrix·Code128)까지 매 프레임 뒤지느라 시간이 **두 배**로 들었다. 폰에서 한 바퀴에 10초 가까이 걸렸을 것 | 카메라는 탑승권 3형식(QR·PDF417·Aztec)만 본다(`zxHints(Z, true)`) + 화면을 1280까지만 줄여서 본다(`SCAN_CAP_LIVE`). 합쳐서 **2.4배 빨라졌다.** 사진 갈래는 시간에 안 쫓기니 5형식·원래 크기 그대로 |
| 사용자가 "안 된다"고만 하고 화면 정보를 못 줌 | 상태 글씨를 카메라 **아래**에 적어서 스크롤 밖이라 못 보고 지나감 | 카메라 화면 **위에 겹쳐서** 보여준다 (`#flLive`). 스캔 화면을 찍으면 무조건 같이 찍힌다 |
| **다섯 번째로 여전히 안 읽힘** | 초점을 고쳐도 그 폰의 미리보기는 여전히 흐렸다(실측 또렷함 13.9 ≈ 2.7px 뭉개짐). 미리보기 화질은 우리가 어떻게 할 수 없는 영역 | **`[📸 찍어서 읽기]` 버튼 신설** — 폰 사진 앱으로 직접 찍게 한다(`capture="environment"`). 사진 앱은 초점을 제대로 잡으므로 확실하다. + 화면에 **또렷함 점수와 판정**을 띄워 사용자가 스스로 고칠 수 있게 했다 |
| **네 번째로 여전히 안 읽힘** — 상태줄은 `81회 · 1080×1920 · 밝기 141` (루프도 화면도 정상) | **초점이었다.** 카메라 미리보기가 살짝만 흐려도 QR 은 통째로 안 읽힌다. 실측: **2px만 뭉개지면 QR 이 500px 이어도 못 읽는다** (해상도를 아무리 올려도 소용없음). 반대로 또렷하면 150px 로도 읽힌다 | ① **선명화**(`unsharp` 언샤프 마스크)를 갈래에 넣었다 — 2px 뭉개짐이 살아난다 ② 카메라를 **3840×2160으로 요청**한다(전엔 1920×1080) ③ 화면을 **누르면 초점 다시**(`refocus`) ④ 줄이기보다 **잘라내기(zoom)를 앞세운다** — 줄이면 QR 한 칸의 픽셀이 같이 줄어든다 |
| 종이 탑승권이 번들거리거나 굽어 있으면 안 읽힘 | 화면 **전체**의 밝기 차이만 벌려서(`boostContrast`) 한쪽만 밝은 사진을 못 잡음 | **자리마다 밝기를 따로 재서** 검정/흰색을 가른다 (`adaptiveBin` · 적분영상이라 창이 커도 픽셀당 한 번). 갈래마다 **줄이는 크기(`max`)도 다르게** 한다 — 같은 사진도 크기에 따라 읽히고 안 읽힌다 |
| 화면이 정신없다 | 큰 그라데이션 버튼 4개 + 떠다니는 버튼 2개가 겹치고, 탭 12개가 여러 줄로 흩어지고, 머리말이 화면 한 장을 먹었다 | 타일 격자로 통일 + 탭은 한 줄 스크롤 + 머리말은 두 줄로 줄이고 긴 설명은 `사용법` 접이식으로 |
| 접이식 메뉴를 눌러도 안 열림 | 여는 연결(`setupFold`)이 `render()` 안에 들어가 **화면을 다시 그릴 때마다 중복 등록**됐다. 두 번 걸리면 한 번 누를 때 두 번 뒤집혀 그대로다 | 시작할 때 한 번만 걸고(`init`), `head.dataset.fold`로 두 번 걸리는 것도 막는다 |
| [🎟 롯데렌터카] 버튼을 눌러도 아무것도 안 뜸 | `#lotteModal.open`에 `display:flex` 규칙이 없었음 | 규칙 추가 + 높이 제한·내부 스크롤 추가 |
| 할인율 칸이 줄을 다 먹어 가게 이름이 세로로 찌그러짐 | `.modal-box input{width:100%}`가 `.lt-in`보다 우선 | `#lotteBody input.lt-in`으로 되돌림 |

### 건드리면 안 되는 것

- **`SCAN_PASSES` 9가지를 줄이지 말 것.** 인쇄된 탑승권은 한 방식으로만 보면 자주 놓친다.
  실제 탑승권 사진으로 재보면 촬영 거리마다 **이기는 갈래가 매번 다르다**
- **기기 내장 인식기만 믿지 말 것.** 되는 것처럼 보이다가 조용히 아무것도 못 읽는 기기가 있다.
  ZXing 갈래는 항상 같이 돌린다
- **기기 내장 인식기를 부를 때는 반드시 `withTimeout`으로 감쌀 것.** `await`만 걸어두면
  답이 없는 기기에서 스캔 전체가 멎는다. 이게 두 번이나 사용자를 막았다
- **카메라 갈래에 형식을 더 넣지 말 것.** 탑승권은 QR·PDF417·Aztec 셋뿐이다.
  형식 하나가 늘 때마다 폰에서 한 바퀴 도는 시간이 눈에 띄게 길어지고,
  **느린 건 사용자에게 "안 되는 것"과 똑같이 보인다**
- **선명화(`unsharp`) 갈래를 빼지 말 것.** 카메라 미리보기는 초점이 조금만 나가도
  QR 이 통째로 안 읽힌다. 이게 실제로 사용자를 막은 **진짜 원인**이었다
- **카메라 화면을 줄여서(`max`) 맞추려 하지 말 것.** 줄이면 QR 한 칸에 배정된 픽셀이
  같이 줄어 흐린 화면이 아예 안 읽힌다. 속도가 필요하면 **잘라내기(`zoom`)**를 쓸 것
- **스캔 순서를 뒤집지 말 것** (카메라 먼저 → 인식기 나중). 아이폰에서 권한이 거부된다
- **탑승권 정보를 공유 저장소에 올리지 말 것.** 탑승객 이름·예약번호가 들어 있어서
  각자 기기에만 저장한다 (`jeju_flights_v1`). 도장·일정은 공유(`jeju_data_v2` + TextDB)지만 항공편은 아니다
- **TextDB 커플 키를 바꾸지 말 것.** 바꾸면 두 사람의 도장이 전부 사라진다
- 저장 병합에서 `w`(가보고 싶은 곳)·`r`(할인율) 칸을 빠뜨리지 말 것

### 탑승권 바코드에 대해 (설계 전제)

- 바코드는 국제 규격 **IATA BCBP**다. 편명·공항·날짜·좌석·예약번호·이름은 들어 있지만
  **출발 시각과 탑승구는 규격에 아예 칸이 없다.** 그래서 출발 시각은 사용자가 한 번 넣고,
  탑승구는 실시간 조회 링크로 넘긴다. 이 전제를 바꾸려 하지 말 것
- 카운터·탑승구 번호는 **당일 배정**이라 코드에 적지 않는다. "전광판 확인"으로 안내한다
- 이름 칸 길이를 어기는 비표준 탑승권이 있어서 `bcbpShift`가 밀린 만큼 보정한다

## 확인하는 법

```bash
npx http-server -p 8897 -s .        # 사이트 띄우기 → http://127.0.0.1:8897/index.html
```

문법 검사 (고치고 나면 항상):
```bash
python3 -c "
import io,re
s=io.open('index.html',encoding='utf-8').read()
io.open('/tmp/f.js','w',encoding='utf-8').write(re.search(r'<script>\n(.*)\n</script>', s, re.S).group(1))
" && node --check /tmp/f.js
```

탑승권 인식 검증 (인식 쪽을 건드렸으면 반드시):
```bash
pip install qrcode pillow pdf417gen
python3 tools/make-test-passes.py /tmp/passes    # 시험 이미지 20장 생성
node tools/verify-scan.mjs /tmp/passes           # 실제 스캔 경로로 확인
```
지금 기준 **16/20 통과**, 실시간 카메라 흐름 통과, **내장 인식기 고장 흉내 4가지 통과**
(빈손 / 오류 / 답이 영영 없음 / 형식 조회부터 답이 없음).
이보다 떨어지면 뭔가 망가진 것이다.
(실패하는 4건은 심하게 흐린 종이 PDF417 — 위 "남은 일" 참고)

브라우저 자동 확인이 필요하면 Playwright를 쓴다. 이 컨테이너에는 Chromium이 이미 깔려 있고
(`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`) **카메라 장치는 없다.** 카메라 흐름을 시험하려면
`tools/verify-scan.mjs`처럼 캔버스 `captureStream()`으로 가짜 MediaStream을 만들어 물리면 된다.

## 관련 저장소

| 저장소 | 담는 것 |
|---|---|
| `goldlucky7/jeju` (여기) | 웹사이트 본체 |
| `goldlucky7/Happy` | 스킬 문서 (`travel-stampbook` 만드는 법 · `stampbook-skill-sync` 동기화 기록) |

**여기 코드를 고쳤으면 Happy 쪽 스킬도 같이 갱신할 것.** 사용자가 "스킬 업데이트했어"라고
하면 `stampbook-skill-sync` 스킬 절차를 따른다 (되묻지 말고 커밋 로그로 직접 찾아낼 것).
`Happy`의 `template-state.md`에 **마지막 반영 커밋**이 적혀 있으니 그 이후 diff를 보면 된다.

새 지역판(부산 등)도 이 저장소 안에 `busan.html` 형태로 함께 올린다.
만드는 방법은 `Happy`의 `travel-stampbook` 스킬에 전부 적혀 있다.
